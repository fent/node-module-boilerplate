{
  "name": basename.replace(/^node-/, ''),
  "description": prompt(),
  "keywords": ["one"],
  "version": "0.1.0",
  "repository": {
    "type": "git",
    "url": "git://github.com/fent/" + basename + ".git"
  },
  "author": "fent (https://github.com/fent)",
  "main": "./lib/index.js",
  "files": [
    "lib"
  ],
  "scripts": {
    "test": "istanbul cover node_modules/.bin/_mocha -- test/*-test.js"
  },
  "dependencies": {},
  "devDependencies": {
    "istanbul": "*",
    "mocha": "*"
  },
  "engines": {
    "node": ">=6"
  },
  "license": "MIT"
}
